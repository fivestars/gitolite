from fsqa.readability import ReadabilityChecker


class QtChecker(ReadabilityChecker):
    readability = """
        ram*
    """.split()

    expected_extension = '.py'

    def __call__(self):
        return (super(QtChecker, self).__call__() and
                'PySide' in self.file_contents)
